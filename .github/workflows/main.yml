name: Blob storage website CI

on:
    push:
        branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: azure/login@v1
      with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Upload to blob storage
      uses: azure/CLI@v1
      with:
        inlineScript: |
<<<<<<< HEAD
            az storage blob upload-batch --account-name cloudcvstore --auth-mode login -d '$web' -s .
=======
            az storage blob upload-batch --account-name cloudcvstore --auth-mode key -d '$web' -s . --overwrite
>>>>>>> 20d1515261142d6ae5ed690811061c054b6f6061
            git 
    - name: Purge CDN endpoint
      uses: azure/CLI@v1
      with:
        inlineScript: |
           az cdn endpoint purge --content-paths  "/*" --profile-name "cloudcvcdn" --name "cloudcv" --resource-group "cloudcv-rg"

  # Azure logout
    - name: logout
      run: |
            az logout
      if: always()
